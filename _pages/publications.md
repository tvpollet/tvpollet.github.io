---
layout: page
permalink: /publications/
title: publications
description: Publications by categories in reversed chronological order. Generated by jekyll-scholar.
nav: true
nav_order: 3
---

<style>
  /* Fix year label readability */
  h2.bibliography {
    opacity: 0.35 !important;
  }
</style>

<!-- bibliography inserted by jekyll-scholar -->

<script>
// Colour abbreviation badges by venue — WCAG AA compliant (all ≥ 4.5:1 on white)
document.addEventListener("DOMContentLoaded", function () {
  var colours = [
    "#2196F3", // blue
    "#388E3C", // green
    "#9C27B0", // purple
    "#D84315", // deep orange
    "#00796B", // teal
    "#C2185B", // dark pink
    "#3F51B5", // indigo
    "#E65100", // burnt orange
    "#546E7A", // blue grey
    "#5D4037", // brown
    "#00695C", // dark teal
    "#673AB7", // deep purple
    "#C62828", // dark red
    "#01579B", // navy blue
    "#33691E", // dark green
    "#AD1457", // magenta
    "#0277BD", // ocean blue
    "#558B2F"  // olive
  ];

  function hash(str) {
    var h = 0;
    for (var i = 0; i < str.length; i++) {
      h = ((h << 5) - h + str.charCodeAt(i)) | 0;
    }
    return Math.abs(h);
  }

  document.querySelectorAll(".abbr abbr").forEach(function (badge) {
    var text = badge.textContent.trim();
    badge.style.backgroundColor = colours[hash(text) % colours.length];
  });
});
</script>
